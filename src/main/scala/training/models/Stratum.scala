package training.models

/** Un Estrato (Personal ocupado).
 *
 *  @constructor crea un Estrato
 *  @param id identificador del Estrato
 *  @param name nombre del Estrato
 */
case class Stratum(id: Int, name: String)